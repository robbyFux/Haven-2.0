# Alembic configuration for Haven Cloud.
# The sqlalchemy.url value here is a placeholder; it is overridden at runtime
# by alembic/env.py which reads DATABASE_URL from pydantic-settings.

[alembic]
script_location = alembic
prepend_sys_path = .

# Template for migration file names
file_template = %%(rev)s_%%(slug)s

# Max length of auto-generated slug in file names
truncate_slug_length = 40

# Set to true to run the environment during the 'revision' command, allowing
# --autogenerate to detect current database state.
revision_environment = false

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
