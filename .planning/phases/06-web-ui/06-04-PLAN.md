---
phase: 06-web-ui
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - server/webui/events/filters.py
  - server/webui/events/views.py
  - server/webui/events/urls.py
  - server/webui/events/templates/events/list.html
  - server/webui/events/templates/events/partials/event_table.html
  - server/webui/events/templates/events/partials/event_row.html
  - server/webui/events/templates/events/detail.html
  - server/webui/tests/test_events.py
autonomous: true
requirements:
  - WEBUI-03
must_haves:
  truths:
    - "Logged-in user sees only their own events, newest first"
    - "User can filter events by date range, device, event type, and severity"
    - "HTMX filter form updates the event table without full page reload"
    - "User can view event detail with trigger list and AI analysis results"
    - "User can play unencrypted video inline; encrypted video shows download message"
    - "User can delete an event (updates user quota counters)"
  artifacts:
    - path: "server/webui/events/views.py"
      provides: "Event list, detail, video serve, and delete views"
    - path: "server/webui/events/filters.py"
      provides: "EventFilter using django-filter"
    - path: "server/webui/tests/test_events.py"
      provides: "Tests for event browser"
  key_links:
    - from: "server/webui/events/views.py"
      to: "server/webui/events/models.py"
      via: "Event.objects.filter(user_id=request.user.id).select_related('device')"
    - from: "server/webui/events/views.py"
      to: "server/webui/events/filters.py"
      via: "EventFilter(request.GET, queryset=...)"
---

<objective>
Event browser with HTMX-powered filters, event detail page with triggers and AI analysis, video playback (unencrypted only), and event deletion.

Purpose: Implements WEBUI-03. Users can browse, filter, inspect, and manage their security events from the web interface.
Output: Event list with filters, event detail page, video serving view, delete action.
</objective>

<context>
@.planning/phases/06-web-ui/06-RESEARCH.md
@.planning/phases/06-web-ui/06-01-SUMMARY.md

<interfaces>
From server/webui/events/models.py (created in Plan 01):
  Event: id, user (FK), device (FK), event_type, severity, timestamp, sensor_value,
         media_path, media_size_bytes, is_encrypted, created_at, updated_at
  EventTrigger: id, event (FK), trigger_type, sensor_value, media_path, created_at, updated_at
  AnalysisResult: id, event (FK, unique), backend, labels (TextField), confidence,
                  description (TextField), raw_result (TextField), created_at, updated_at

From server/webui/devices/models.py:
  Device: id, user (FK), name, app_key, is_active

Event types (from TriggerType): ACCELEROMETER, CAMERA, MICROPHONE, PRESSURE, LIGHT,
  POWER, BUMP, CAMERA_VIDEO, HEART, CAMERA_PERSON, CAMERA_PET, CAMERA_VEHICLE,
  SOUND_DECIBEL, GYROSCOPE

Severity values: LOW, MEDIUM, HIGH, CRITICAL

MEDIA_ROOT: /app/media (Docker volume shared with FastAPI)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Event list with django-filter and HTMX partial rendering</name>
  <files>
    server/webui/events/filters.py
    server/webui/events/views.py
    server/webui/events/urls.py
    server/webui/events/templates/events/list.html
    server/webui/events/templates/events/partials/event_table.html
    server/webui/events/templates/events/partials/event_row.html
    server/webui/tests/test_events.py
  </files>
  <behavior>
    - test_event_list_requires_login: GET /events/ without auth -> redirect to login
    - test_event_list_shows_own_events: user sees own events ordered by -timestamp
    - test_event_list_excludes_other_users: user does not see other users' events
    - test_event_filter_by_severity: GET /events/?severity=HIGH -> only HIGH events
    - test_event_filter_by_device: GET /events/?device_id=X -> only events from that device
    - test_event_filter_by_date_range: GET /events/?timestamp__gte=...&timestamp__lte=... -> filtered
    - test_event_filter_by_type: GET /events/?event_type=CAMERA -> only camera events
    - test_event_list_htmx_partial: GET with HX-Request -> returns only event_table.html
    - test_event_list_pagination: 30 events, page_size=25 -> page 1 has 25, page 2 has 5
  </behavior>
  <action>
1. Create events/filters.py:
   - EventFilter(django_filters.FilterSet):
     - timestamp__gte: DateTimeFilter(field_name="timestamp", lookup_expr="gte", label="From")
     - timestamp__lte: DateTimeFilter(field_name="timestamp", lookup_expr="lte", label="To")
     - event_type: CharFilter(field_name="event_type", lookup_expr="iexact")
     - severity: ChoiceFilter(choices=[("LOW","Low"),("MEDIUM","Medium"),("HIGH","High"),("CRITICAL","Critical")])
     - device_id: NumberFilter(field_name="device_id")
     - Meta: model = Event, fields as above

2. Create events/views.py:
   - event_list(request): @login_required
     base_qs = Event.objects.filter(user_id=request.user.id).select_related("device").order_by("-timestamp")
     f = EventFilter(request.GET, queryset=base_qs)
     paginator = Paginator(f.qs, 25)
     page = paginator.get_page(request.GET.get("page", 1))
     
     Also fetch user's devices for the filter dropdown:
     devices = Device.objects.filter(user_id=request.user.id, is_active=True)
     
     context = {"page": page, "filter": f, "devices": devices}
     if request.htmx: return render(request, "events/partials/event_table.html", context)
     return render(request, "events/list.html", context)

3. Create events/urls.py:
   - app_name = "events"
   - path "" -> event_list (name="list")

4. Create templates:
   - list.html (extends base.html):
     Title: "Events"
     Filter form at top in a bg-gray-800 card:
       - Date range inputs (type="date"), device dropdown, event type dropdown, severity dropdown
       - Form attributes: hx-get="{% url 'events:list' %}", hx-target="#event-table", hx-trigger="change, submit", hx-push-url="true"
       - Filter button (teal) + Reset link
     Event table below in div#event-table: {% include "events/partials/event_table.html" %}

   - partials/event_table.html:
     Table headers: Time, Device, Type, Severity, Sensor Value, Media, Actions
     Loop: {% for event in page %} include partials/event_row.html {% endfor %}
     Pagination row: page N of M, prev/next links with hx-get + hx-target="#event-table" + hx-push-url
     Empty state: "No events found" if page is empty
     Show total count: "{{ page.paginator.count }} events"

   - partials/event_row.html:
     <tr> with event data
     Time: monospace, formatted as "YYYY-MM-DD HH:MM:SS"
     Device: device.name
     Type: event_type with icon (camera icon for CAMERA*, mic for MICROPHONE, etc.)
     Severity: colored badge — LOW=teal, MEDIUM=amber, HIGH=orange, CRITICAL=red
       LOW: bg-teal-900/50 text-teal-400
       MEDIUM: bg-amber-900/50 text-amber-400
       HIGH: bg-orange-900/50 text-orange-400
       CRITICAL: bg-red-900/50 text-red-400
     Media: if media_path: video icon link to detail; if is_encrypted: lock icon
     Actions: "View" link to event detail

   Tailwind: table with divide-y divide-gray-700, text-sm, bg-gray-800 header

5. Write tests. Create test events using direct model creation. For pagination test: create 30 events, check page counts.
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0/server/webui" && python -m pytest tests/test_events.py -x -v -k "list or filter or pagination or partial" 2>&1 | tail -25</automated>
  </verify>
  <done>
    - Event list shows user's events in descending timestamp order
    - All 4 filter types work (severity, device, type, date range)
    - HTMX partial rendering returns only table HTML
    - Pagination works with 25 events per page
    - 9+ tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Event detail, video serving, and event deletion</name>
  <files>
    server/webui/events/views.py
    server/webui/events/urls.py
    server/webui/events/templates/events/detail.html
    server/webui/tests/test_events.py
  </files>
  <behavior>
    - test_event_detail_shows_triggers: GET /events/<id>/ -> page shows event info + all triggers
    - test_event_detail_shows_analysis: event with AnalysisResult -> page shows labels, confidence, description
    - test_event_detail_no_analysis: event without AnalysisResult -> "No AI analysis" message
    - test_event_detail_other_user: GET another user's event -> 404
    - test_serve_video_unencrypted: GET /events/<id>/video/ for unencrypted event -> 200 with video/mp4
    - test_serve_video_encrypted: GET /events/<id>/video/ for encrypted event -> 403 with message
    - test_serve_video_no_media: GET /events/<id>/video/ for event without media -> 404
    - test_event_delete: POST /events/<id>/delete/ -> event deleted, user counters updated, redirect to list
    - test_event_delete_other_user: POST to another user's event -> 404
  </behavior>
  <action>
1. Add to events/views.py:
   - event_detail(request, event_id): @login_required
     event = get_object_or_404(Event, id=event_id, user_id=request.user.id)
     triggers = EventTrigger.objects.filter(event_id=event.id).order_by("created_at")
     analysis = AnalysisResult.objects.filter(event_id=event.id).first()
     Render events/detail.html with event, triggers, analysis

   - serve_video(request, event_id): @login_required
     event = get_object_or_404(Event, id=event_id, user_id=request.user.id)
     if not event.media_path: raise Http404
     if event.is_encrypted:
       return HttpResponse("This video was client-encrypted and cannot be played in the browser. Please download to decrypt locally.", status=403, content_type="text/plain")
     full_path = os.path.join(settings.MEDIA_ROOT, event.media_path)
     if not os.path.exists(full_path): raise Http404
     response = StreamingHttpResponse(open(full_path, "rb"), content_type="video/mp4")
     response["Content-Length"] = os.path.getsize(full_path)
     response["Content-Disposition"] = f'inline; filename="event_{event_id}.mp4"'
     return response

   - event_delete(request, event_id): @login_required, POST only
     event = get_object_or_404(Event, id=event_id, user_id=request.user.id)
     # Delete media file if exists
     if event.media_path:
       full_path = os.path.join(settings.MEDIA_ROOT, event.media_path)
       if os.path.exists(full_path):
         os.remove(full_path)
     # Update user counters
     user = request.user
     if event.media_size_bytes:
       user.current_storage_bytes = max(0, user.current_storage_bytes - (event.media_size_bytes or 0))
     user.current_event_count = max(0, user.current_event_count - 1)
     user.save(update_fields=["current_storage_bytes", "current_event_count"])
     # Delete event (cascades triggers + analysis in DB)
     event.delete()
     messages.success(request, "Event deleted.")
     return redirect("events:list")

2. Add URL patterns:
   - path("<int:event_id>/", event_detail, name="detail")
   - path("<int:event_id>/video/", serve_video, name="video")
   - path("<int:event_id>/delete/", event_delete, name="delete")

3. Create events/detail.html (extends base.html):
   Layout: two-column grid on large screens, single column on mobile

   Left column (event info card, bg-gray-800 rounded-lg p-6):
   - Event type + severity badge (same color scheme as list)
   - Timestamp (monospace)
   - Device name
   - Sensor value
   - If media_path and not is_encrypted: HTML5 <video> tag with src pointing to serve_video URL, controls, class="w-full rounded-lg"
   - If media_path and is_encrypted: yellow warning box "This video is client-encrypted. Download to decrypt locally." + download link
   - If no media_path: "No media attached"
   - Delete button: red, POST to delete URL with hx-confirm="Delete this event permanently?"

   Right column:
   - Triggers card (bg-gray-800):
     List of triggers with trigger_type, sensor_value, timestamp
     Each trigger as a small card/row

   - AI Analysis card (bg-gray-800):
     If analysis exists:
       Backend badge (tflite/openrouter)
       Labels: parse JSON labels string, show as colored chips
       Confidence: percentage with progress bar
       Description: if present, show as blockquote
     If no analysis: "No AI analysis available" muted text

4. Write tests. For video tests: create a temp file in a temp MEDIA_ROOT, set event.media_path. Use pytest tmp_path or tempfile. For delete test: verify event is gone and user counters updated.
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0/server/webui" && python -m pytest tests/test_events.py -x -v -k "detail or video or delete" 2>&1 | tail -25</automated>
  </verify>
  <done>
    - Event detail shows all triggers and AI analysis
    - Unencrypted video streams with content-type video/mp4
    - Encrypted video returns 403 with explanation message
    - Event delete removes row, media file, and updates quota counters
    - User isolation enforced on all views (404 for other users' events)
    - 9+ tests pass
  </done>
</task>

</tasks>

<verification>
1. `cd server/webui && python -m pytest tests/test_events.py -v` -- all tests pass
2. Manual: browse events, apply filters, check HTMX partial updates
3. Manual: view event detail with triggers and analysis results
4. Manual: delete event, verify removal from list
</verification>

<success_criteria>
- Event list with 4-way filtering (date, device, type, severity) via django-filter
- HTMX partial rendering for filter + pagination (no full page reload)
- Event detail shows triggers, AI analysis (labels, confidence, description)
- Video playback for unencrypted events; clear message for encrypted
- Event deletion updates user quota counters
- User isolation on all endpoints
- 18+ tests pass across list + detail + video + delete
</success_criteria>

<output>
After completion, create `.planning/phases/06-web-ui/06-04-SUMMARY.md`
</output>
