---
phase: 08-android-webui-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/havenapp/main/detection/SensorFusionEngine.kt
  - app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt
  - app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
  - app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt
  - app/src/main/java/org/havenapp/main/detection/PerceptualHashDetector.kt
  - app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt
  - app/src/main/java/org/havenapp/main/MonitorService.kt
  - app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt
  - app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "All inline code comments in the 8 target Kotlin files are in English (no German text remains)"
    - "SensorFusionEngine, LightMonitor, FusedMotionMonitor, CameraAnalyzer, PerceptualHashDetector, and HavenObjectDetector each contain WHY explanations for their core algorithmic choices"
    - "Public functions in the 6 hot-spot files have KDoc @param/@return tags where the parameter name or return value is non-obvious"
    - "SettingsScreen.kt contains HorizontalDivider between each adjacent pair of SettingsSection composables inside multi-section CategoryCards, with no divider after the last section"
    - "App compiles without errors after all edits"
  artifacts:
    - path: "app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt"
      provides: "HorizontalDivider separators between SettingsSections"
      contains: "import androidx.compose.material3.HorizontalDivider"
    - path: "app/src/main/java/org/havenapp/main/detection/SensorFusionEngine.kt"
      provides: "English comments + WHY explanations for Complementary Filter"
    - path: "app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt"
      provides: "English comments + WHY for dual-rate EMA + cross-sensor suppression"
    - path: "app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt"
      provides: "English comments + WHY for SENSOR_DELAY_GAME + 90th-percentile noise floor"
    - path: "app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt"
      provides: "English comments + WHY for 3-stage pipeline gating"
    - path: "app/src/main/java/org/havenapp/main/detection/PerceptualHashDetector.kt"
      provides: "English comments + WHY for DCT-based perceptual hashing"
    - path: "app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt"
      provides: "Translated German KDoc and inline comments + WHY for EfficientDet Lite 0 choice"
    - path: "app/src/main/java/org/havenapp/main/MonitorService.kt"
      provides: "Translated German inline comments (3 comments at lines ~217, ~263, ~328)"
    - path: "app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt"
      provides: "Translated German inline comments (2 comments at lines ~121, ~157)"
  key_links:
    - from: "SettingsScreen.kt"
      to: "HorizontalDivider import"
      via: "import androidx.compose.material3.HorizontalDivider"
      pattern: "import androidx\\.compose\\.material3\\.HorizontalDivider"
    - from: "SettingsScreen.kt"
      to: "CategoryCard bodies"
      via: "HorizontalDivider between SettingsSections, not after the last"
      pattern: "HorizontalDivider\\(modifier = Modifier\\.padding\\(vertical = 8\\.dp\\)\\)"
---

<objective>
Standardize all Android Kotlin source documentation to English and add algorithmic WHY comments
to the 6 hot-spot detection/sensor files. Also add a HorizontalDivider between SettingsSection
composables inside multi-section CategoryCards in SettingsScreen.kt.

Purpose: Makes the codebase readable without German language knowledge; captures algorithmic
insight in comments so future maintainers understand why algorithms are designed as they are.
The divider change improves visual grouping in the Settings screen.

Output: 9 modified Kotlin source files. No new files. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-android-webui-ux-polish/08-CONTEXT.md
@.planning/phases/08-android-webui-ux-polish/08-PATTERNS.md
@.planning/phases/08-android-webui-ux-polish/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate German comments + add WHY/KDoc in hot-spot detection files</name>

  <read_first>
    app/src/main/java/org/havenapp/main/detection/SensorFusionEngine.kt
    app/src/main/java/org/havenapp/main/detection/PerceptualHashDetector.kt
    app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt
    app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt
    app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
    app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt
  </read_first>

  <files>
    app/src/main/java/org/havenapp/main/detection/SensorFusionEngine.kt,
    app/src/main/java/org/havenapp/main/detection/PerceptualHashDetector.kt,
    app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt,
    app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt,
    app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt,
    app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt
  </files>

  <action>
Read all 6 files in full, then apply the following changes to each:

**SensorFusionEngine.kt** — Already English. Add KDoc @param/@return to processAccelerometer (params: x, y, z axis accelerometer values in m/s²; return: Euclidean delta magnitude) and processGyroscope (params: x, y, z angular velocity in rad/s). The class-level KDoc is already complete — do not change it. The fuse() function already has sufficient inline doc.

**PerceptualHashDetector.kt** — If any German comments exist, translate them. Add WHY explanation for the DCT-based approach: the 8x8 average hash reduces an image to 64-bit structural fingerprint; Hamming distance ≥ 4 (configurable) is robust against JPEG artifacts and minor lighting changes while catching structural motion. Add KDoc @param/@return to the public hash() and distance() functions where not already present.

**HavenObjectDetector.kt** — Translate ALL German text to English. German is confirmed at: class-level KDoc (lines 19–35), isAvailable StateFlow doc (line 56), loadModel() KDoc (line 67), inline step comments (lines 71, 84), detect() inline KDoc (line 108). After translating, add WHY explanations: EfficientDet Lite 0 chosen for ~4 MB size and 16 KB page-size alignment (Android 16 compat); RunningMode.IMAGE for synchronous per-frame inference (avoids callback threading on the ImageAnalysis executor); graceful degradation when model file is absent (detect() returns empty list, initError holds cause).

**LightMonitor.kt** — Already English with good WHY comments. Review for any remaining German text (none expected per PATTERNS.md but verify). Add @param/@return to any public function missing them where the parameter or return is non-obvious. Do not add boilerplate tags to self-explanatory parameters.

**FusedMotionMonitor.kt** — Translate German comment on _noiseFloor: "Wird auf den berechneten Noise-Floor gesetzt, sobald der Warmup abgeschlossen ist." → "Set to the computed noise floor once warmup is complete; null during warmup." Add KDoc to observe(): "@param sensitivity Sensitivity level controlling the threshold multiplier. @param warmupMs Warmup duration in ms; motion events are suppressed until calibration completes. @param expert Optional expert thresholds overriding the default sensitivity multipliers." Also add WHY inline comments for: SENSOR_DELAY_GAME choice (20 ms vs 200 ms polling, ~10x faster, acceptable battery trade-off for security use case); 90th-percentile noise floor calculation (more robust than mean — rejects brief spikes during calibration warm-up that would inflate the mean and create an unrealistically high baseline).

**CameraAnalyzer.kt** — Translate ALL German text to English. German confirmed at: class-level KDoc (lines 26–39), TFLite throttle doc (line 57), inline comments at lines ~85, ~103, ~111, ~144, ~147, ~162, ~165, cropLuma KDoc (~lines 184–186), buildBitmap KDoc (~lines 203–205). After translating, expand the class-level KDoc WHY explanation (see PATTERNS.md §CameraAnalyzer.kt section for the full "Three-stage camera motion detection pipeline" English text to use). WHY for luma-first gate: cheap to compute, no bitmap allocation; WHY for pHash gate: filters uniform brightness changes that fool pixel diff; WHY for TFLite throttle (TFLITE_MIN_INTERVAL_MS): prevents OOM on continuous inference; stage gating saves ~80% of inference calls.

**Style rules for all files:**
- Language: English only. Sentence fragments acceptable for inline comments (no trailing period required).
- WHY format: explain reason the value/approach was chosen, not what the code does.
- KDoc: "/** … */" block style. Add @param only where parameter name is non-obvious. Add @return only for non-trivial return values. Never add boilerplate tags for self-documenting parameters like "enabled: Boolean".
- Do not restructure code — only edit comment text and KDoc blocks.
  </action>

  <verify>
    <automated>grep -rn "Stufe\|Schritt\|Kamera\|Erkennung\|Schwelle\|Empfindlichkeit\|Kalibrierung\|Modell\|Klassen\|Datei\|Laden\|erkann\|berechneten\|abgeschlossen" app/src/main/java/org/havenapp/main/detection/ app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt 2>/dev/null | grep -v "^Binary" | wc -l</automated>
    Result must be 0 (no German words remaining in these 6 files).
    Supplementary umlaut/ß check (catches German words not in the word list above):
    grep -rn --include="*.kt" "[üöäÜÖÄß]" app/src/main/java/org/havenapp/main/detection/ app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt app/src/main/java/org/havenapp/main/media/CameraAnalyzer.kt 2>/dev/null | grep -v "^Binary" | grep "//" | wc -l
    Result must be 0 (no German umlaut or ß characters inside comments in these 6 files; note: the grep filters to comment lines only via "//" to avoid false positives in string literals or identifiers).
    Additionally: grep -c "HorizontalDivider\|@param\|@return" app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt must be > 0.
  </verify>

  <acceptance_criteria>
    - grep for German words (listed above) across the 6 files returns 0 matches
    - HavenObjectDetector.kt contains no German text in any comment or KDoc block
    - CameraAnalyzer.kt class-level KDoc contains "Three-stage" or "three-stage" in English
    - FusedMotionMonitor.kt contains the string "noise floor" in English in the _noiseFloor doc
    - FusedMotionMonitor.kt observe() function has a KDoc block with @param sensitivity and @param warmupMs
    - SensorFusionEngine.kt processAccelerometer has @param annotations
    - ./gradlew :app:compileDebugKotlin exits 0
  </acceptance_criteria>

  <done>All 6 hot-spot files: zero German comments; WHY explanations present in class-level KDoc or inline for each file's core algorithm; @param/@return KDoc added for non-obvious public function signatures. Compile passes.</done>
</task>

<task type="auto">
  <name>Task 2: Translate German comments in MonitorService.kt and ZoneEditorScreen.kt; add HorizontalDivider in SettingsScreen.kt</name>

  <read_first>
    app/src/main/java/org/havenapp/main/MonitorService.kt
    app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt
    app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
  </read_first>

  <files>
    app/src/main/java/org/havenapp/main/MonitorService.kt,
    app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt,
    app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
  </files>

  <action>
**MonitorService.kt — translate 3 German inline comments only (no WHY expansion, no KDoc changes):**

Find and replace these exact German strings with their English equivalents:
- "// Countdown während Kalibrierung" → "// Countdown while calibrating"
- "// Nach Kalibrierungszeit DB-Event öffnen und Zustand wechseln" → "// After calibration period: open DB event and transition state to ACTIVE"
- "// Sensor-Flow läuft ab Kalibrierungsstart; currentEventId ist null" → "// Sensor flow runs from calibration start; currentEventId is null until state transitions to ACTIVE"

Scan the entire file for any other German inline comments not in the list above and translate them. Do not add KDoc or WHY explanations to MonitorService.kt — it is translate-only per CONTEXT.md scope.

**ZoneEditorScreen.kt — translate 2 German inline comments only:**

Find and replace:
- "// Querformat: Kamera links, Steuerung rechts" → "// Landscape: camera on the left, controls on the right"
- "// Hochformat: Kamera oben, Steuerung unten" → "// Portrait: camera on top, controls below"

Scan entire file for any other German comments and translate. No KDoc or WHY changes.

**SettingsScreen.kt — add HorizontalDivider between SettingsSection composables (per D-06, D-07):**

1. Add import at the top of the file: `import androidx.compose.material3.HorizontalDivider` — insert it in the `androidx.compose.material3.*` import group (alphabetical order; after HorizontalDivider comes no `H` entry, so place it after the last `androidx.compose.material3` import or within the wildcard block if a wildcard is used).

2. Read the CategoryCard bodies in the composable function. For each CategoryCard that contains 2 or more SettingsSection composable calls, insert `HorizontalDivider(modifier = Modifier.padding(vertical = 8.dp))` between each adjacent pair of SettingsSection call sites. Do NOT add a divider after the last SettingsSection in any card.

3. The pattern from PATTERNS.md for Card 1 (Detection card) shows SettingsSections for: Sensitivity, Camera, Detection, Zone, expert_entry_label, Sensors, LightSuppress — insert HorizontalDivider between each adjacent pair, omit after LightSuppress (last one). Apply the same logic to every other CategoryCard with multiple sections (Cards 2–5 or however many exist in the file).

4. Do not modify any SettingsSection composable definition itself, any CategoryCard composable definition, any existing Row composables, or any other code outside CategoryCard body lambdas.

Rule: The divider call is exactly: `HorizontalDivider(modifier = Modifier.padding(vertical = 8.dp))`
No color parameter — let M3 resolve to MaterialTheme.colorScheme.outline by default (as specified in UI-SPEC.md).
  </action>

  <verify>
    <automated>./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
    Exit code 0. Also verify:
    grep -c "HorizontalDivider" app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
    Must be >= 2 (import + at least one usage).
  </verify>

  <acceptance_criteria>
    - MonitorService.kt: grep for "Kalibrierung\|während\|Zustand\|wechseln\|currentEventId ist" returns 0 matches
    - ZoneEditorScreen.kt: grep for "Querformat\|Hochformat\|Steuerung" returns 0 matches
    - SettingsScreen.kt contains "import androidx.compose.material3.HorizontalDivider"
    - SettingsScreen.kt contains at least one "HorizontalDivider(modifier = Modifier.padding(vertical = 8.dp))" call
    - No HorizontalDivider appears immediately before the closing brace of any CategoryCard (no trailing divider after last SettingsSection)
    - ./gradlew :app:compileDebugKotlin exits 0
  </acceptance_criteria>

  <done>MonitorService.kt and ZoneEditorScreen.kt contain no German inline comments. SettingsScreen.kt has HorizontalDivider between each adjacent pair of SettingsSections in every multi-section CategoryCard, with none after the last section. Kotlin compile passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file → compiler | Edited Kotlin source files are processed by the Kotlin compiler; malformed syntax would cause build failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Kotlin source comments | accept | Comment-only changes have no runtime attack surface; no executable logic is modified |
| T-08-02 | Tampering | HorizontalDivider composable insertion | accept | UI-only change; no data processed; M3 component with no external input |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete, run the full compile check:
```
./gradlew :app:compileDebugKotlin
```
Must exit 0 with no errors.

Spot-check German-free state:
```
grep -rn "Schritt\|Stufe\|Kamera-Bewegungserkennung\|Drei-Stufen\|Luminanz\|Perceptual Hash\|TFLite-Drosselung\|Kalibrierungszeit\|Querformat\|Hochformat" \
  app/src/main/java/org/havenapp/main/detection/ \
  app/src/main/java/org/havenapp/main/sensor/ \
  app/src/main/java/org/havenapp/main/media/ \
  app/src/main/java/org/havenapp/main/MonitorService.kt \
  app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt
```
Must return 0 matches.

Verify HorizontalDivider is present and imported:
```
grep -c "HorizontalDivider" app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
```
Must be >= 2.
</verification>

<success_criteria>
1. All inline code comments in the 9 target Kotlin files are in English — zero German words remain in comment or KDoc text.
2. SensorFusionEngine, LightMonitor, FusedMotionMonitor, CameraAnalyzer, PerceptualHashDetector, and HavenObjectDetector each contain WHY explanations for their core algorithmic choices (alpha=0.7, dual-rate EMA, SENSOR_DELAY_GAME, 3-stage pipeline gating, DCT hash, EfficientDet Lite 0).
3. Public functions in the 6 hot-spot files have KDoc @param/@return tags where the parameter name or return value is non-obvious; boilerplate tags for self-documenting parameters are absent.
4. SettingsScreen.kt: HorizontalDivider(modifier = Modifier.padding(vertical = 8.dp)) appears between every adjacent pair of SettingsSections in every multi-section CategoryCard; no trailing divider after the last section in any card.
5. ./gradlew :app:compileDebugKotlin exits 0.
</success_criteria>

<output>
Create .planning/phases/08-android-webui-ux-polish/08-01-SUMMARY.md when done.
</output>
