---
phase: 04-notification-engine
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-de/strings.xml
autonomous: true
requirements:
  - NOTIF-04

must_haves:
  truths:
    - "SettingsScreen groups existing settings into five category Cards (Erkennung, Aufzeichnung, Sicherheit, Benachrichtigungen, App)"
    - "Each Card uses surfaceVariant containerColor and RoundedCornerShape(12.dp)"
    - "Existing SettingsSection blocks are preserved inside Cards as sub-headers"
    - "Benachrichtigungen Card has placeholder content for channel config (filled by plan 05)"
    - "App Card contains a LogLevel toggle and existing language/diagnostics/version sections"
  artifacts:
    - path: "app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt"
      provides: "Card-grouped settings layout with CategoryCard composable"
      contains: "CategoryCard"
    - path: "app/src/main/res/values/strings.xml"
      provides: "English strings for category headers and notification labels"
      contains: "settings_cat_detection"
    - path: "app/src/main/res/values-de/strings.xml"
      provides: "German translations for new strings"
      contains: "settings_cat_detection"
  key_links:
    - from: "SettingsScreen.kt"
      to: "SettingsViewModel"
      via: "collectAsStateWithLifecycle for new notification settings"
      pattern: "collectAsStateWithLifecycle"
---

<objective>
Restructure SettingsScreen from flat sections into five category Cards (per D-14, D-15, D-16), add string resources for new sections, and add a LogLevel toggle in the App card.

Purpose: Visual grouping improves discoverability and creates the container where notification settings (plan 05) will be placed.
Output: SettingsScreen with Card-based layout, all new string resources for Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-notification-engine/04-CONTEXT.md
@.planning/phases/04-notification-engine/04-RESEARCH.md
@.planning/phases/04-notification-engine/04-01-SUMMARY.md

<interfaces>
<!-- SettingsScreen existing composables to preserve -->

From SettingsScreen.kt (reusable composables):
```kotlin
@Composable fun SettingsSection(title: String, content: @Composable () -> Unit)
@Composable fun SensorToggleRow(label: String, enabled: Boolean, onToggle: (Boolean) -> Unit)
@Composable fun RadioRow(text: String, selected: Boolean, onClick: () -> Unit)
```

From SettingsViewModel.kt:
```kotlin
data class SettingsUiState(
    val sensitivity, cameraPosition, countdownSeconds, calibrationSeconds,
    languageTag, detectionMode, detectionZone, tfliteAvailable,
    motionEnabled, lightEnabled, micEnabled, cameraEnabled,
    clipDurationSeconds, mediaEncryptionEnabled
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 string resources (EN + DE)</name>
  <files>
    app/src/main/res/values/strings.xml,
    app/src/main/res/values-de/strings.xml
  </files>
  <read_first>
    app/src/main/res/values/strings.xml,
    app/src/main/res/values-de/strings.xml
  </read_first>
  <action>
    Add the following strings to `values/strings.xml` (before the closing `</resources>` tag):

    ```xml
    <!-- Settings Category Cards (Phase 4, D-14/D-16) -->
    <string name="settings_cat_detection">Detection</string>
    <string name="settings_cat_recording">Recording</string>
    <string name="settings_cat_security">Security</string>
    <string name="settings_cat_notifications">Notifications</string>
    <string name="settings_cat_app">App</string>

    <!-- Notification Settings (Phase 4) -->
    <string name="settings_signal_title">Signal</string>
    <string name="settings_signal_configure">Configure Signal</string>
    <string name="settings_signal_enabled">Signal enabled</string>
    <string name="settings_signal_server_url">Server URL</string>
    <string name="settings_signal_sender">Sender number</string>
    <string name="settings_signal_recipient">Recipient number</string>
    <string name="settings_signal_bearer_token">Bearer token (optional)</string>
    <string name="settings_signal_configured">Configured: %1$s</string>
    <string name="settings_signal_not_configured">Not configured</string>

    <string name="settings_mattermost_title">Mattermost</string>
    <string name="settings_mattermost_configure">Configure Mattermost</string>
    <string name="settings_mattermost_enabled">Mattermost enabled</string>
    <string name="settings_mattermost_webhook_url">Webhook URL</string>
    <string name="settings_mattermost_configured">Configured: %1$s</string>
    <string name="settings_mattermost_not_configured">Not configured</string>

    <string name="settings_notification_rule_title">Alert rules</string>
    <string name="settings_min_severity">Minimum severity</string>
    <string name="settings_cooldown">Cooldown</string>
    <string name="cooldown_off">Off</string>
    <string name="cooldown_1min">1 minute</string>
    <string name="cooldown_5min">5 minutes</string>
    <string name="cooldown_15min">15 minutes</string>
    <string name="cooldown_30min">30 minutes</string>
    <string name="settings_trigger_types">Trigger types</string>
    <string name="settings_attach_media">Attach image (Signal only)</string>

    <string name="settings_heartbeat_title">Heartbeat</string>
    <string name="heartbeat_off">Off</string>
    <string name="heartbeat_15min">15 minutes</string>
    <string name="heartbeat_30min">30 minutes</string>
    <string name="heartbeat_60min">60 minutes</string>

    <!-- Log Level (Phase 4, D-17) -->
    <string name="settings_log_level_title">Log level</string>
    <string name="log_level_normal">Normal</string>
    <string name="log_level_debug">Debug</string>

    <!-- Dialog buttons -->
    <string name="dialog_save">Save</string>
    <string name="dialog_cancel">Cancel</string>
    <string name="dialog_test">Test</string>
    ```

    Add the corresponding German translations to `values-de/strings.xml`:

    ```xml
    <!-- Settings Category Cards (Phase 4) -->
    <string name="settings_cat_detection">Erkennung</string>
    <string name="settings_cat_recording">Aufzeichnung</string>
    <string name="settings_cat_security">Sicherheit</string>
    <string name="settings_cat_notifications">Benachrichtigungen</string>
    <string name="settings_cat_app">App</string>

    <!-- Notification Settings (Phase 4) -->
    <string name="settings_signal_title">Signal</string>
    <string name="settings_signal_configure">Signal konfigurieren</string>
    <string name="settings_signal_enabled">Signal aktiviert</string>
    <string name="settings_signal_server_url">Server-URL</string>
    <string name="settings_signal_sender">Absendernummer</string>
    <string name="settings_signal_recipient">Empfaengernummer</string>
    <string name="settings_signal_bearer_token">Bearer-Token (optional)</string>
    <string name="settings_signal_configured">Konfiguriert: %1$s</string>
    <string name="settings_signal_not_configured">Nicht konfiguriert</string>

    <string name="settings_mattermost_title">Mattermost</string>
    <string name="settings_mattermost_configure">Mattermost konfigurieren</string>
    <string name="settings_mattermost_enabled">Mattermost aktiviert</string>
    <string name="settings_mattermost_webhook_url">Webhook-URL</string>
    <string name="settings_mattermost_configured">Konfiguriert: %1$s</string>
    <string name="settings_mattermost_not_configured">Nicht konfiguriert</string>

    <string name="settings_notification_rule_title">Alarmregeln</string>
    <string name="settings_min_severity">Mindestschweregrad</string>
    <string name="settings_cooldown">Abklingzeit</string>
    <string name="cooldown_off">Aus</string>
    <string name="cooldown_1min">1 Minute</string>
    <string name="cooldown_5min">5 Minuten</string>
    <string name="cooldown_15min">15 Minuten</string>
    <string name="cooldown_30min">30 Minuten</string>
    <string name="settings_trigger_types">Ausloesertypen</string>
    <string name="settings_attach_media">Bild anhaengen (nur Signal)</string>

    <string name="settings_heartbeat_title">Heartbeat</string>
    <string name="heartbeat_off">Aus</string>
    <string name="heartbeat_15min">15 Minuten</string>
    <string name="heartbeat_30min">30 Minuten</string>
    <string name="heartbeat_60min">60 Minuten</string>

    <string name="settings_log_level_title">Log-Level</string>
    <string name="log_level_normal">Normal</string>
    <string name="log_level_debug">Debug</string>

    <string name="dialog_save">Speichern</string>
    <string name="dialog_cancel">Abbrechen</string>
    <string name="dialog_test">Testen</string>
    ```
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0" && grep -c "settings_cat_" app/src/main/res/values/strings.xml && grep -c "settings_cat_" app/src/main/res/values-de/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - `values/strings.xml` contains `settings_cat_detection`, `settings_cat_recording`, `settings_cat_security`, `settings_cat_notifications`, `settings_cat_app`
    - `values/strings.xml` contains `settings_signal_title`, `settings_mattermost_title`, `settings_notification_rule_title`
    - `values/strings.xml` contains `settings_log_level_title`, `log_level_normal`, `log_level_debug`
    - `values-de/strings.xml` contains all corresponding German translations
    - Both files have equal count of `settings_cat_` entries (5 each)
  </acceptance_criteria>
  <done>All Phase 4 string resources exist in both EN and DE</done>
</task>

<task type="auto">
  <name>Task 2: Restructure SettingsScreen with CategoryCard containers and LogLevel toggle</name>
  <files>
    app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt,
    app/src/main/java/org/havenapp/main/ui/settings/SettingsViewModel.kt,
    app/src/main/java/org/havenapp/main/storage/AppLogger.kt
  </read_first>
  <action>
    **1. Add a `CategoryCard` composable** (private, inside the same file, after the existing `SettingsSection` composable):

    ```kotlin
    @Composable
    private fun CategoryCard(
        title: String,
        modifier: Modifier = Modifier,
        content: @Composable ColumnScope.() -> Unit,
    ) {
        Card(
            modifier = modifier.fillMaxWidth(),
            colors = CardDefaults.cardColors(
                containerColor = MaterialTheme.colorScheme.surfaceVariant,
            ),
            shape = RoundedCornerShape(12.dp),
        ) {
            Column(modifier = Modifier.padding(16.dp)) {
                Text(
                    text = title,
                    style = MaterialTheme.typography.titleMedium,
                    color = MaterialTheme.colorScheme.onSurfaceVariant,
                )
                Spacer(Modifier.height(12.dp))
                content()
            }
        }
    }
    ```

    Add necessary imports: `Card`, `CardDefaults`, `ColumnScope`, `RoundedCornerShape`.

    **2. Restructure the `Column` body in `SettingsScreen`:**

    Replace the flat `SettingsSection` list with `CategoryCard` wrappers. Change the Column's `verticalArrangement` to `Arrangement.spacedBy(16.dp)` and remove existing `HorizontalDivider` separators between sections.

    The five cards map existing sections as follows (per D-16):

    **Card 1 — Erkennung/Detection** (`stringResource(R.string.settings_cat_detection)`):
    - SettingsSection: Sensitivity (radio buttons)
    - SettingsSection: Camera (front/back)
    - SettingsSection: Detection mode (radio buttons + AI availability)
    - SettingsSection: Detection zone (edit button)
    - SettingsSection: Active sensors (toggle rows)
    - SettingsSection: Light suppress on motion

    **Card 2 — Aufzeichnung/Recording** (`stringResource(R.string.settings_cat_recording)`):
    - SettingsSection: Start delay (countdown)
    - SettingsSection: Calibration duration
    - SettingsSection: Clip duration

    **Card 3 — Sicherheit/Security** (`stringResource(R.string.settings_cat_security)`):
    - SettingsSection: PIN (enable/change)
    - SettingsSection: Auto-lock
    - SettingsSection: Media encryption

    **Card 4 — Benachrichtigungen/Notifications** (`stringResource(R.string.settings_cat_notifications)`):
    - For now: add a placeholder `Text("—", style = MaterialTheme.typography.bodyMedium)` — plan 05 will fill in Signal/Mattermost/Rule config.
    - Add a comment: `// TODO: Plan 05 adds Signal, Mattermost, rule config, and heartbeat sections here`

    **Card 5 — App** (`stringResource(R.string.settings_cat_app)`):
    - SettingsSection: Language
    - SettingsSection: Log level — new section with a `SensorToggleRow` or `Switch`:
      ```kotlin
      SettingsSection(title = stringResource(R.string.settings_log_level_title)) {
          Row(
              modifier = Modifier.fillMaxWidth(),
              horizontalArrangement = Arrangement.SpaceBetween,
              verticalAlignment = Alignment.CenterVertically,
          ) {
              Text(
                  text = if (logLevelDebug) stringResource(R.string.log_level_debug)
                         else stringResource(R.string.log_level_normal),
                  style = MaterialTheme.typography.bodyMedium,
              )
              Switch(
                  checked = logLevelDebug,
                  onCheckedChange = { viewModel.setLogLevel(it) },
              )
          }
      }
      ```
    - SettingsSection: Diagnostics (open button)
    - SettingsSection: About Haven (version info)

    **3. Add `logLevelDebug` state collection in the composable:**
    ```kotlin
    val logLevelDebug by viewModel.logLevelDebug.collectAsStateWithLifecycle()
    ```
    (This StateFlow will be added to SettingsViewModel in plan 05; for now, add a temporary `val logLevelDebug = MutableStateFlow(false)` placeholder in SettingsViewModel — OR connect it directly to SettingsRepository.logLevel flow. The simpler approach: read it from SettingsViewModel in plan 05. For now, keep the Switch but wire it to a local `remember { mutableStateOf(false) }` placeholder so the screen compiles. Comment: `// TODO: Plan 05 wires to SettingsViewModel.logLevelDebug`)

    **4. Ensure the overall Column structure remains `Column + verticalScroll`** (NOT LazyColumn). Keep `rememberScrollState()`. Add `Spacer(Modifier.height(16.dp))` after the last card for bottom padding.
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsScreen.kt` contains `CategoryCard` composable with `surfaceVariant` containerColor and `RoundedCornerShape(12.dp)`
    - `SettingsScreen.kt` contains `stringResource(R.string.settings_cat_detection)`, `settings_cat_recording`, `settings_cat_security`, `settings_cat_notifications`, `settings_cat_app`
    - `SettingsScreen.kt` does NOT contain `LazyColumn`
    - `SettingsScreen.kt` contains `verticalScroll(rememberScrollState())`
    - `SettingsScreen.kt` contains `Arrangement.spacedBy(16.dp)` on the outer Column
    - `SettingsScreen.kt` contains `settings_log_level_title` reference
    - `./gradlew :app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <done>SettingsScreen displays five category Cards with existing sections properly grouped inside, plus a LogLevel toggle in the App card. Notification card has placeholder for plan 05.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:compileDebugKotlin` passes
- SettingsScreen has 5 CategoryCard instances
- String resources exist in both EN and DE
- No HorizontalDivider between major sections (replaced by Card spacing)
</verification>

<success_criteria>
SettingsScreen visually groups settings into five cards. All Phase 4 string resources are available. Layout compiles and is ready for notification UI additions in plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/04-notification-engine/04-03-SUMMARY.md`
</output>
