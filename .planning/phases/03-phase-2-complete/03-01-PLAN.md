---
phase: 03-phase-2-complete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
autonomous: true
requirements:
  - TFLITE-01
  - TFLITE-02
  - TFLITE-03
  - ZONE-01
  - ZONE-02
  - EVENT-01
  - EVENT-02
  - SENSOR-01
  - SENSOR-02

must_haves:
  truths:
    - "App compiles without errors after all quick-task commits"
    - "TFLite model is initialized before any screen renders (HavenApplication eager init)"
    - "Detection modes PERSON/PET/VEHICLE/ALL are selectable when model is present"
    - "Detection zone drawn in ZoneEditorScreen survives app restart"
    - "Events can be deleted from Timeline and EventDetailScreen"
    - "FusedMotionMonitor does not emit triggers during calibration warmup"
    - "CameraAnalyzer runs without crashes in ML mode"
  artifacts:
    - path: "app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt"
      provides: "Calibration-phase trigger suppression"
      contains: "warmupMs"
  key_links:
    - from: "FusedMotionMonitor.observe()"
      to: "MonitorService sensorFlow.collect"
      via: "Flow<TriggerEvent>"
      pattern: "fusedMotionMonitor.observe"
---

<objective>
Verify all already-fixed Phase 2 bugs compile and work, then add SENSOR-02 calibration guard to FusedMotionMonitor.

Purpose: Confirm that quick-task fixes (TFLite eager init, zone persistence, event deletion) are integrated and the app compiles. Add warmup-period trigger suppression to FusedMotionMonitor so no spurious triggers fire during calibration.
Output: Verified compilation, FusedMotionMonitor with calibration guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-phase-2-complete/03-RESEARCH.md
@app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
@app/src/main/java/org/havenapp/main/MonitorService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify compilation of all quick-task fixes</name>
  <files>none (verification only)</files>
  <read_first>
    app/build.gradle.kts
    app/src/main/java/org/havenapp/main/MonitorService.kt
    app/src/main/java/org/havenapp/main/detection/HavenObjectDetector.kt
    app/src/main/java/org/havenapp/main/ui/settings/ZoneEditorScreen.kt
    app/src/main/java/org/havenapp/main/ui/timeline/EventDetailScreen.kt
  </read_first>
  <action>
Run `./gradlew :app:compileDebugKotlin` to verify the project compiles after all quick-task commits.

Confirms requirements already addressed by prior quick tasks:
- TFLITE-01, TFLITE-02, TFLITE-03: HavenApplication calls `objectDetector.initialize()` on daemon thread; SettingsViewModel also calls it; DiagnosticsViewModel uses `availabilityFlow`.
- ZONE-01, ZONE-02: ZoneEditorScreen saves zone to DataStore; MonitorService reads it at start.
- EVENT-01, EVENT-02: Swipe-to-delete on Timeline, delete button in EventDetailScreen, ForeignKey CASCADE.
- SENSOR-01: CameraAnalyzer guards with `objectDetector?.isAvailable == true` and `TFLITE_MIN_INTERVAL_MS` throttle.

If compilation fails, diagnose and fix the error. Otherwise, proceed to Task 2.
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :app:compileDebugKotlin` exits with code 0
    - Output contains "BUILD SUCCESSFUL"
  </acceptance_criteria>
  <done>Project compiles successfully, confirming TFLITE-01/02/03, ZONE-01/02, EVENT-01/02, SENSOR-01 code is integrated.</done>
</task>

<task type="auto">
  <name>Task 2: Add calibration-phase trigger suppression to FusedMotionMonitor (SENSOR-02)</name>
  <files>app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt</files>
  <read_first>
    app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
    app/src/main/java/org/havenapp/main/sensor/SensorMonitor.kt
    app/src/main/java/org/havenapp/main/sensor/LightMonitor.kt
    app/src/main/java/org/havenapp/main/MonitorService.kt
  </read_first>
  <action>
In `FusedMotionMonitor.observe(sensitivity, warmupMs)`, the `warmupMs` parameter indicates the calibration duration. During this warmup period the monitor should collect noise-floor samples but NOT emit `TriggerEvent`s to the downstream flow.

Current architecture: MonitorService only persists triggers when `currentEventId != null` (which is null during calibration). This means triggers emitted during warmup are already silently dropped at the Service level. However, for defense-in-depth (SENSOR-02), FusedMotionMonitor should also suppress trigger emission during warmup internally.

Implementation:
1. Read the current `observe()` function to understand the flow structure.
2. Add a `val warmupDeadline = System.currentTimeMillis() + warmupMs` at the top of the `callbackFlow` block.
3. In the threshold-exceeded branch (where `TriggerEvent` is created and sent via `trySend`), add a guard: `if (System.currentTimeMillis() < warmupDeadline) return@...` (use appropriate return label for the sensor callback lambda). This skips trigger emission during warmup while still allowing noise-floor calculation.
4. Verify `./gradlew :app:compileDebugKotlin` passes.

Do NOT change the noise-floor calculation logic -- it must still run during warmup. Only suppress the `trySend(TriggerEvent(...))` call.
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `FusedMotionMonitor.kt` contains a warmup deadline guard (grep for `warmupDeadline` or `warmupMs` used in a time comparison)
    - The guard is INSIDE the `callbackFlow` block, BEFORE the `trySend(TriggerEvent` call
    - Noise-floor calculation code (`noiseFloor`, `_noiseFloor`, percentile) is NOT gated by the warmup check
    - `./gradlew :app:compileDebugKotlin` exits with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>FusedMotionMonitor suppresses trigger emission during warmup period while still calculating noise floor. SENSOR-02 addressed.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:compileDebugKotlin` passes
- FusedMotionMonitor.kt contains warmup guard
- All 9 requirements (TFLITE-01/02/03, ZONE-01/02, EVENT-01/02, SENSOR-01/02) are verified or implemented
</verification>

<success_criteria>
- Compilation succeeds with all prior fixes integrated
- FusedMotionMonitor has calibration-phase trigger suppression
- No regressions in existing sensor/detection code
</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-2-complete/03-01-SUMMARY.md`
</output>
