---
phase: 03-phase-2-complete
plan: 06
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "FusedMotionMonitor registers accelerometer and gyroscope with SENSOR_DELAY_GAME (~20ms) instead of SENSOR_DELAY_NORMAL (~200ms)"
  artifacts:
    - path: "app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt"
      provides: "Faster sensor sampling for motion detection"
      contains: "SENSOR_DELAY_GAME"
  key_links: []
---

<objective>
Change sensor registration delay from SENSOR_DELAY_NORMAL to SENSOR_DELAY_GAME in FusedMotionMonitor
for both accelerometer and gyroscope, reducing motion detection latency from ~200ms to ~20ms.

Purpose: User reported motion sensor reacts too slowly (per D-06). SENSOR_DELAY_GAME provides
~10x faster sampling with acceptable battery impact for a security monitoring app.
Output: Modified FusedMotionMonitor with faster sensor rate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-phase-2-complete/03-CONTEXT.md

<interfaces>
<!-- From FusedMotionMonitor.kt lines 128-131 -- the two lines to change -->
```kotlin
// Line 128:
sensorManager.registerListener(accelListener, accelSensor, SensorManager.SENSOR_DELAY_NORMAL)
// Line 130 (inside if block):
sensorManager.registerListener(gyroListener, gyroSensor, SensorManager.SENSOR_DELAY_NORMAL)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change sensor delay from NORMAL to GAME in FusedMotionMonitor</name>
  <files>
    app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
  </files>
  <read_first>
    app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt
  </read_first>
  <action>
Two-line change in `FusedMotionMonitor.kt`. No other modifications to this file.

**Line 128** -- change accelerometer registration:
```
Before: sensorManager.registerListener(accelListener, accelSensor, SensorManager.SENSOR_DELAY_NORMAL)
After:  sensorManager.registerListener(accelListener, accelSensor, SensorManager.SENSOR_DELAY_GAME)
```

**Line 130** (inside the `if (gyroSensor != null)` block) -- change gyroscope registration:
```
Before: sensorManager.registerListener(gyroListener, gyroSensor, SensorManager.SENSOR_DELAY_NORMAL)
After:  sensorManager.registerListener(gyroListener, gyroSensor, SensorManager.SENSOR_DELAY_GAME)
```

Per D-06 and D-07: no other changes to FusedMotionMonitor. Warmup, noise floor (90th percentile),
multipliers, cooldown logic, and fusion algorithm remain unchanged.

Verify: `./gradlew :app:compileDebugKotlin`
  </action>
  <verify>
    <automated>cd "/home/user/Dokumente/Projekte/Haven 2.0" && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `FusedMotionMonitor.kt` contains `SensorManager.SENSOR_DELAY_GAME` on two lines (accel and gyro registration)
    - `FusedMotionMonitor.kt` does NOT contain `SENSOR_DELAY_NORMAL`
    - No other changes in the file (warmup, fusion, thresholds all unchanged)
    - `./gradlew :app:compileDebugKotlin` exits with BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>FusedMotionMonitor uses SENSOR_DELAY_GAME for both accelerometer and gyroscope.</done>
</task>

</tasks>

<verification>
- `./gradlew :app:compileDebugKotlin` passes
- `grep -c "SENSOR_DELAY_GAME" app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt` returns 2
- `grep -c "SENSOR_DELAY_NORMAL" app/src/main/java/org/havenapp/main/sensor/FusedMotionMonitor.kt` returns 0
</verification>

<success_criteria>
- Motion detection responds within ~20ms instead of ~200ms (D-06)
- No changes to warmup, noise floor, or fusion algorithm (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/03-phase-2-complete/03-06-SUMMARY.md`
</output>
