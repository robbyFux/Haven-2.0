---
phase: quick
plan: 260406-v3d
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/havenapp/main/notify/SignalIntentChannel.kt
  - app/src/main/java/org/havenapp/main/storage/SettingsRepository.kt
  - app/src/main/java/org/havenapp/main/ui/settings/SettingsViewModel.kt
  - app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
  - app/src/main/java/org/havenapp/main/MonitorService.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values-de/strings.xml
autonomous: true
requirements: []

must_haves:
  truths:
    - "SignalIntentChannel.kt is deleted from the notify/ package"
    - "SettingsRepository has no signal_intent_* keys, flows, or setters"
    - "SettingsViewModel has no signalIntent* StateFlows or setter functions"
    - "SettingsScreen has no Signal (App) section, no showSignalIntentDialog state, no SignalIntentConfigDialog composable"
    - "MonitorService reads no signalIntent settings and instantiates no SignalIntentChannel"
    - "strings.xml (EN + DE) contain no settings_signal_intent_* keys"
    - "Project compiles without errors"
  artifacts:
    - path: "app/src/main/java/org/havenapp/main/notify/SignalIntentChannel.kt"
      provides: "DELETED — must not exist"
    - path: "app/src/main/java/org/havenapp/main/storage/SettingsRepository.kt"
      provides: "No signal_intent references"
    - path: "app/src/main/java/org/havenapp/main/ui/settings/SettingsViewModel.kt"
      provides: "No signalIntent* members"
    - path: "app/src/main/java/org/havenapp/main/MonitorService.kt"
      provides: "No SignalIntentChannel import or instantiation"
  key_links:
    - from: "MonitorService.kt"
      to: "notify/SignalIntentChannel.kt"
      via: "import + instantiation"
      pattern: "SignalIntentChannel"
---

<objective>
Remove SignalIntentChannel entirely from the codebase.

Purpose: Signal has no public Android intent API for automated sending. The signal.me deep link is iOS-only. Android 10+ blocks background activity starts from services. The implementation was architecturally unsound and is being deleted, not revised.

Output: All seven files cleaned of every SignalIntentChannel reference. Project compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete SignalIntentChannel.kt and clean backend files (SettingsRepository, SettingsViewModel, MonitorService)</name>
  <files>
    app/src/main/java/org/havenapp/main/notify/SignalIntentChannel.kt
    app/src/main/java/org/havenapp/main/storage/SettingsRepository.kt
    app/src/main/java/org/havenapp/main/ui/settings/SettingsViewModel.kt
    app/src/main/java/org/havenapp/main/MonitorService.kt
  </files>
  <action>
**1. DELETE the file** `app/src/main/java/org/havenapp/main/notify/SignalIntentChannel.kt` entirely.

**2. SettingsRepository.kt** — remove these items (lines 54–56 and 258–269 of current file):
- Remove the two companion object keys: `KEY_SIGNAL_INTENT_ENABLED` and `KEY_SIGNAL_INTENT_RECIPIENT` (and the comment "SignalIntent fallback channel" above them)
- Remove the entire "Signal Intent channel" block (the comment `// ── Signal Intent channel` through the closing brace of `setSignalIntentRecipient`): remove the two Flow properties `signalIntentEnabled` and `signalIntentRecipient`, and the two suspend setters `setSignalIntentEnabled` and `setSignalIntentRecipient`

**3. SettingsViewModel.kt** — remove these items (lines 282–287 and 332–338 of current file):
- Remove the two StateFlow declarations: `signalIntentEnabled` and `signalIntentRecipient`
- Remove the two setter functions: `setSignalIntentEnabled(enabled: Boolean)` and `setSignalIntentRecipient(recipient: String)`
- No other ViewModel members reference these; the rest of the file is untouched

**4. MonitorService.kt** — remove these items:
- Remove the import on line 52: `import org.havenapp.main.notify.SignalIntentChannel`
- Remove the two settings snapshot lines (lines 149–150): `val signalIntentEnabled = settingsRepository.signalIntentEnabled.first()` and `val signalIntentRecipient = settingsRepository.signalIntentRecipient.first()`
- Remove the `if (signalIntentEnabled)` block (lines 165–167) inside the `buildList` lambda:
  ```kotlin
  if (signalIntentEnabled) {
      add(SignalIntentChannel(applicationContext, signalIntentRecipient))
  }
  ```
  The surrounding `buildList` and the other two `if` blocks (signalEnabled, mattermostEnabled) remain intact.
  </action>
  <verify>
    <automated>grep -rn "SignalIntent\|signal_intent\|signalIntent" app/src/main/java/org/havenapp/main/notify/ app/src/main/java/org/havenapp/main/storage/SettingsRepository.kt app/src/main/java/org/havenapp/main/ui/settings/SettingsViewModel.kt app/src/main/java/org/havenapp/main/MonitorService.kt 2>/dev/null | grep -v "^Binary" && echo "FAIL: references remain" || echo "OK: no references"</automated>
  </verify>
  <done>
    - SignalIntentChannel.kt does not exist
    - SettingsRepository.kt has no KEY_SIGNAL_INTENT_* keys, no signalIntentEnabled/signalIntentRecipient flows, no setSignalIntent* functions
    - SettingsViewModel.kt has no signalIntent* StateFlows or setter functions
    - MonitorService.kt has no SignalIntentChannel import, no signalIntentEnabled/signalIntentRecipient snapshot reads, no SignalIntentChannel instantiation
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean SettingsScreen UI and string resources, then verify compile</name>
  <files>
    app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt
    app/src/main/res/values/strings.xml
    app/src/main/res/values-de/strings.xml
  </files>
  <action>
**1. SettingsScreen.kt** — four surgical removals:

**a) Lines 81–82** — remove the two `collectAsStateWithLifecycle` state reads:
```kotlin
val signalIntentEnabled by viewModel.signalIntentEnabled.collectAsStateWithLifecycle()
val signalIntentRecipient by viewModel.signalIntentRecipient.collectAsStateWithLifecycle()
```

**b) Line 92** — remove the dialog state variable:
```kotlin
var showSignalIntentDialog by remember { mutableStateOf(false) }
```

**c) Lines 397–420** — remove the entire "Signal Intent fallback channel" UI block inside the Notifications card:
```kotlin
// Signal Intent fallback channel
SettingsSection(title = stringResource(R.string.settings_signal_intent_title)) {
    SensorToggleRow(...)
    OutlinedButton(...) { Text(...) }
    Text(
        text = stringResource(R.string.settings_signal_intent_note),
        ...
    )
}
```
The blank line before the `// NotificationRule (D-05)` comment (line 422) and everything after it remains.

**d) Lines 598–608** — remove the Signal Intent dialog invocation block:
```kotlin
// Signal Intent recipient dialog
if (showSignalIntentDialog) {
    SignalIntentConfigDialog(
        initialRecipient = signalIntentRecipient,
        onDismiss = { showSignalIntentDialog = false },
        onSave = { recipient ->
            viewModel.setSignalIntentRecipient(recipient)
            showSignalIntentDialog = false
        },
    )
}
```

**e) Lines 823–871** — remove the entire `SignalIntentConfigDialog` private composable function (from `@Composable` annotation through the closing `}` brace at line 871).

After all removals, check for any now-unused imports (e.g. nothing else uses `mutableStateOf` or `remember` only for the intent dialog — verify both are still used elsewhere before removing). Do NOT blindly remove imports that are still referenced by the remaining Signal/Mattermost dialog state variables (`showSignalDialog`, `showMattermostDialog`).

**2. strings.xml (EN)** — remove the block on lines 185–193:
```xml
<!-- Signal Intent channel (P2 fallback) -->
<string name="settings_signal_intent_title">Signal (App)</string>
<string name="settings_signal_intent_enabled">Signal App enabled</string>
<string name="settings_signal_intent_configure">Configure Signal App</string>
<string name="settings_signal_intent_configured">Recipient: %1$s</string>
<string name="settings_signal_intent_not_configured">Not configured</string>
<string name="settings_signal_intent_recipient">Recipient number (E.164)</string>
<string name="settings_signal_intent_dialog_hint">Requires Signal app installed. Opens Signal compose screen on alert. Heartbeat not supported.</string>
<string name="settings_signal_intent_note">Fallback: opens Signal app on device. No server needed.</string>
```

**3. strings.xml (DE)** — remove the equivalent block on lines 184–192:
```xml
<!-- Signal Intent channel (P2 fallback) -->
<string name="settings_signal_intent_title">Signal (App)</string>
<string name="settings_signal_intent_enabled">Signal-App aktiviert</string>
<string name="settings_signal_intent_configure">Signal-App konfigurieren</string>
<string name="settings_signal_intent_configured">Empfänger: %1$s</string>
<string name="settings_signal_intent_not_configured">Nicht konfiguriert</string>
<string name="settings_signal_intent_recipient">Empfängernummer (E.164)</string>
<string name="settings_signal_intent_dialog_hint">Erfordert installierte Signal-App. Öffnet Signal beim Alarm. Heartbeat nicht unterstützt.</string>
<string name="settings_signal_intent_note">Fallback: öffnet Signal-App auf dem Gerät. Kein Server nötig.</string>
```

**4. Compile check:**
```bash
./gradlew :app:compileDebugKotlin
```
Fix any remaining compile errors before marking done. Common leftover: a stray reference to a removed string resource, or an import that now resolves to nothing.
  </action>
  <verify>
    <automated>grep -rn "SignalIntent\|signal_intent\|signalIntent" app/src/main/java/org/havenapp/main/ui/settings/SettingsScreen.kt app/src/main/res/values/strings.xml app/src/main/res/values-de/strings.xml 2>/dev/null | grep -v "^Binary" && echo "FAIL: references remain" || echo "OK: no references"; ./gradlew :app:compileDebugKotlin --quiet 2>&1 | tail -5</automated>
  </verify>
  <done>
    - SettingsScreen.kt has no signalIntent* state reads, no showSignalIntentDialog, no Signal Intent section in the Notifications card, no SignalIntentConfigDialog invocation, no SignalIntentConfigDialog composable
    - strings.xml (EN + DE) have no settings_signal_intent_* keys
    - `./gradlew :app:compileDebugKotlin` exits 0 with no errors
  </done>
</task>

</tasks>

<verification>
Full codebase sweep — must return empty:
```bash
grep -rn "SignalIntent\|signal_intent\|signalIntent" app/src/main/java/ app/src/main/res/ 2>/dev/null | grep -v "^Binary"
```

Compile check:
```bash
./gradlew :app:compileDebugKotlin
```
</verification>

<success_criteria>
- `grep -rn "SignalIntent\|signal_intent\|signalIntent" app/src/` returns zero matches
- `./gradlew :app:compileDebugKotlin` exits 0
- The notify/ package contains only HavenAlertChannel, MattermostChannel, NotificationRouter, NotificationRule, SignalRestChannel (no SignalIntentChannel)
</success_criteria>

<output>
No SUMMARY.md required for quick tasks.
</output>
